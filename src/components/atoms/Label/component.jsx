import React from "react";
import { LabelWrapper } from "./style";

const Label = ({ text }) => {
    return (
        <LabelWrapper>
            {text}
        </LabelWrapper>);
};

export default Label;
